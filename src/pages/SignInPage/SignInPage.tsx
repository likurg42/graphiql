import Title from '../../components/Title';
import { SignInForm } from './SignInForm';

const SignUpPage = () => {
  return (
    <section>
      <Title>Sign In</Title>
      <SignInForm />
    </section>
  );
};

export default SignUpPage;
